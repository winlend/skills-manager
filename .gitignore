# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript build info
*.tsbuildinfo

# Environment variables
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local agent/session scratch (do not commit)
.claude
.loop-log
reference
docs/_local/

# Personal build merge config (generated by scripts/sync-and-build.ps1)
src-tauri/tauri.personal-build.conf.json

# NOTE (winlend fork): project docs + CLAUDE.md ARE tracked so future
# sessions and clones get fork purpose / upstream-sync context.
# See docs/FORK.md. Upstream historically ignored `docs`/`CLAUDE.md`;
# this fork intentionally tracks them.

