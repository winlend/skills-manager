# Normalize text files to LF in the repo; avoid Windows phantom-dirty
# (status=modified, empty diff) on files like Cargo.toml under core.autocrlf.
* text=auto eol=lf

# Keep Windows scripts with CRLF for native cmd.exe / notepad friends
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
